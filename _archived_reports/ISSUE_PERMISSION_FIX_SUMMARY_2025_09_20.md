# GitHub Actions Issue 权限配置总结

**日期**: 2025-09-20
**状态**: ✅ 已解决

## 问题与解决

### 原始问题
- **错误**: `GraphQL: Resource not accessible by integration (createIssue)`
- **原因**: GitHub Actions 默认 GITHUB_TOKEN 缺少创建 Issue 的权限

### 解决方案（已实施）
**PR #65**: 添加工作流权限声明
```yaml
permissions:
  contents: read   # 读取仓库内容
  issues: write    # 创建和更新 Issue
```

## 验证结果 ✅

### 第一次运行（权限修复前）
- **运行 ID**: 17880981268
- **结果**: 工作流成功，但 Issue 操作失败
- **Issue 创建**: ❌ 失败
- **Issue 评论**: ❌ 失败
- **工件上传**: ✅ 成功

### 第二次运行（权限修复后）
- **运行 ID**: 17881095004
- **结果**: 完全成功
- **Issue 创建**: ✅ 自动使用现有 Issue #64
- **Issue 评论**: ✅ 由 github-actions[bot] 成功创建
- **工件上传**: ✅ 成功

## 权限配置最佳实践

### 1. 最小权限原则
只授予工作流必需的权限：
```yaml
permissions:
  contents: read      # 必需：读取代码
  issues: write       # 必需：创建/更新 Issue
  # pull-requests: write  # 不需要：不操作 PR
```

### 2. 权限层级
```yaml
# 工作流级别（影响所有 jobs）
permissions:
  issues: write

jobs:
  job1:
    # 继承工作流权限

  job2:
    # 可覆盖工作流权限
    permissions:
      issues: read  # 降级为只读
```

### 3. 可用权限列表
| 权限 | 作用域 | 常见用途 |
|------|--------|----------|
| `actions: read/write` | 工作流运行 | 触发其他工作流 |
| `checks: read/write` | 检查运行 | 创建状态检查 |
| `contents: read/write` | 代码仓库 | 读取/提交代码 |
| `deployments: read/write` | 部署 | 创建部署 |
| `issues: read/write` | Issues | 创建/更新 Issue |
| `packages: read/write` | 包 | 发布包 |
| `pull-requests: read/write` | PR | 创建/更新 PR |
| `statuses: read/write` | 提交状态 | 设置提交状态 |

## Daily CI Status 工作流当前配置

```yaml
name: Daily CI Status Report

on:
  schedule:
    - cron: '0 2 * * *'  # 10:00 UTC+8
  workflow_dispatch:

permissions:
  contents: read   # 读取仓库文件和脚本
  issues: write    # 创建和评论 Issue

jobs:
  daily-status:
    runs-on: ubuntu-latest
    steps:
      # 工作流步骤...
```

## 功能验证清单

| 功能 | 修复前 | 修复后 |
|------|--------|--------|
| 手动触发 | ✅ | ✅ |
| 运行监控脚本 | ✅ | ✅ |
| 生成报告 | ✅ | ✅ |
| 上传工件 | ✅ | ✅ |
| 创建 Issue | ❌ | ✅ |
| 更新 Issue | ❌ | ✅ |

## 结论

通过添加明确的权限声明，成功解决了 GitHub Actions 无法操作 Issue 的问题。Daily CI Status Report 工作流现在可以：
1. 自动创建 Issue（如不存在）
2. 自动添加评论到 Issue
3. 由 github-actions[bot] 用户执行操作
4. 保持最小权限原则

## 后续行动

✅ **已完成**：
- PR #65 已合并
- 权限问题已修复
- Issue #64 自动更新功能已验证

📅 **计划**：
- 2025-09-21 10:00 UTC+8 - 观察首次定时运行
- 持续监控 Issue #64 更新情况
- 收集一周数据后评估报告格式

---
**文档生成**: 2025-09-20 23:45 UTC+8