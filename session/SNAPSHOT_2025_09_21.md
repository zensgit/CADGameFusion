# CADGameFusion 会话快照（2025-09-21）

时间：2025-09-21 13:03:17 UTC
范围：延续 2025-09-20 的状态与待办，补充“离线模式”增强建议

## 摘要
- 文档/脚本：准备为本地校验脚本增加离线/无pip模式，便于在受限环境运行。
- 记录：生成今日快照与检查点，保持连续性；准备更新 LATEST 指针。

## 近期状态（承接 09-20）
- v0.2.1 已发布；严格构建/导出验证工作流稳定；Windows 构建阻塞，已加强重试与可观测性。
- PR #66 日报增强等待自动合并；PR #67 导出工具链版本输出为 Draft，待验证后合并。

## 待办（沿用并精炼）
1) 更新金样后复跑严格导出（use_vcpkg=false/true, rtol=1e-6），确认字段/结构一致。
2) 日报首跑验证通过后，将 PR #67 从 Draft 改为 Ready 并合并。
3) 连续绿后考虑将 Strict Exports 设为必需检查。
4) 离线模式：
   - 为 `tools/local_ci.sh` 增加 `--offline` 或 `--no-pip`，跳过 pip 与 schema 校验，仅执行可离线的生成/比对。
   - 为 `scripts/check_verification.sh` 增加轻量参数（例如 `--no-struct`）以跳过结构检查，仅做场景与字段状态快速判定。

## 本地执行建议
- 自检：`bash scripts/dev_env_verify.sh`
- 如需离线快速验证：
  - 构建：`cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_EDITOR_QT=OFF -G Ninja`（如可用）
  - 导出：`build/tools/export_cli --out build/exports --scene sample --gltf-holes full`
  - 快速比对：`python3 tools/compare_export_to_sample.py build/exports/scene_cli_sample sample_exports/scene_sample`

（此快照为静态记录；动态状态以 GitHub Actions 为准）
