# Step 45: Package Consumer CI Smoke - Design

## Goal
- Ensure CI runs the package consumer smoke test after building.

## Changes
1. Run `package_consumer_smoke` in core-strict build tests (Linux).

## Files
- `.github/workflows/core-strict-build-tests.yml`
