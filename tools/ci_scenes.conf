###############################################
# CADGameFusion CI / Local validation scenes  #
###############################################
#
# Format:
#   required=comma,separated,list
#   quick=comma,separated,list (subset for --quick checks)
# Lines beginning with # are ignored.
#
# Normal scenes (exported with --scene):
#   sample, holes, multi, units, complex
# Spec-driven scenes (exported with --spec tools/specs/<name>.json if file exists):
#   scene_complex_spec, scene_concave_spec, scene_nested_holes_spec
#
# If a spec file is missing locally it is silently skipped in export phase.

required=sample,holes,multi,units,complex,scene_complex_spec,scene_concave_spec,scene_nested_holes_spec
quick=sample,units,complex,scene_complex_spec
